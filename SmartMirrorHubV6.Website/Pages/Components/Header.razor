@if (ShowHeader)
{
    <div class="component-header d-flex font-weight-bold">@Name</div>
}

@code {
    [Parameter]
    public string Name { get; set; }

    [Parameter]
    public bool ShowHeader { get; set; }
}
